import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

const Visualizer = () => {
    return (
        <View>
            <Text>This is the visualizer</Text>
        </View>
    );
}

export default Visualizer;