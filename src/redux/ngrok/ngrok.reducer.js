const INITIAL_STATE = {
    ngrokURL: 'https://fed0827ea42d.ngrok.io'
}

const ngrokReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default: 
        return state;
    }
}

export default ngrokReducer;